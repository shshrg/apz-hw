apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: counter-data
  name: counter-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
