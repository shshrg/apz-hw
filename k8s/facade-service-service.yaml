apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: facade-service
  name: facade-service
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    io.kompose.service: facade-service
