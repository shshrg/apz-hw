apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: hazelcast-node
  name: hazelcast-node
spec:
  ports:
    - name: "5701"
      port: 5701
      targetPort: 5701
  selector:
    io.kompose.service: hazelcast-node
