apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: logging-service
  name: logging-service
spec:
  clusterIP: None
  ports:
    - name: "8082"
      port: 8082
      targetPort: 8082
  selector:
    io.kompose.service: logging-service
